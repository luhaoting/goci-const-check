syntax = "proto3";
package example;

option go_package = "goci-const-check/pb";

import "immutable_options.proto";

message Person {
  // 给 id 打 immutable 标注
  int64 id = 1;
  string name = 2;
  int32 age = 3; // 比如年龄不可变（示例）
}
