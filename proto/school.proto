syntax = "proto3";
package example;

option go_package = "goci-const-check/pb/example";

import "immutable_options.proto";
import "person.proto";

message School {
    string name          = 1;
    string address       = 2;
    TeacherTeam teachers = 3 [(field_options.immutable) = true]; 
}

message TeacherTeam {
    map<uint32, Person> teachers = 1;
}